{
  "name": "momo-relay",
  "version": "1.0.0",
  "private": true,
  "description": "momo-anonymous-decision 纯内存 WebSocket 碎片中转（无日志，零留存）",
  "main": "relay.js",
  "scripts": {
    "start": "node relay.js",
    "test": "node matcher.test.js && node cleanup.test.js && node ../utils/crypto.test.js && node smoke.test.js"
  },
  "dependencies": {
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
